apiVersion: v1
kind: Service
metadata:
  name: my-nginx-service
  labels:
    app: my-nginx-service
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  selector:
    app: my-nginx-app